name: flurt_backend
description: Real-time WebSocket server and state synchronization gateway for Flurt Watch Party.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.0
  shelf_cors_headers: ^0.1.5
  http: ^1.2.2
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
